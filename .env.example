# -------------------------------------------------------------------
# Example environment configuration for Log Guardian
# Copy this file to `.env` and adjust values as needed
# -------------------------------------------------------------------

# API settings
LOG_GUARDIAN_HOST=0.0.0.0
LOG_GUARDIAN_PORT=8000
LOG_GUARDIAN_RELOAD=true

# Logging
LOG_GUARDIAN_LOG_LEVEL=INFO
# Set to 1 for JSON logs (structured logging)
LOG_GUARDIAN_LOG_JSON=0
# Optional: write logs to a rotating file
# LOG_GUARDIAN_LOG_FILE=logs/log_guardian.log

# Model paths
LOG_GUARDIAN_MODEL_DIR=models/artifacts
LOG_GUARDIAN_REGISTRY=models/registry.json

# Security
# Example: secret for signing tokens if auth layer is added
LOG_GUARDIAN_SECRET_KEY=change_me_in_prod
# Optional: JWT token expiration time in seconds
LOG_GUARDIAN_JWT_EXPIRATION=3600